{
  "name": "todo-list",
  "version": "1.0.0",
  "private": true,
  "description": "To-Do list app (client + server)",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix server\" \"npm run start --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
